spring:
  datasource:
    url: jdbc:h2:mem:flight_advisor;IGNORECASE=TRUE
    username: ivanm
    password: "Rand0mP4$$word"
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-ui
  jpa:
    show-sql: true
server:
  search:
    depth: 2 #do not set value above 3, it will slow searching of routes