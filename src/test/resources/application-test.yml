spring:
  datasource:
    url: "jdbc:tc:postgres:16-alpine:///localhost?TC_TMPFS=/flight_advisor:rw"
    username: ivanm
    password: rand0mpassword
    driver-class-name: org.postgresql.Driver
  test:
    database:
      replace: none